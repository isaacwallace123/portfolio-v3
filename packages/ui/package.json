{
  "name": "@iw/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared, token-driven React UI for the isaacwallace.dev network. Components style themselves via the design-contract CSS variables each app defines, so one component wears every app's palette. Consumed as source via Next transpilePackages.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "@iw/core": "*",
    "react": ">=19",
    "react-dom": ">=19"
  }
}
