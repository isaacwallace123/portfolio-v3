# ailab.isaacwallace.dev — public AIOps experiment arena. Model execution remains on dedicated
# workers while this stateless frontend exposes curated controls and evidence.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ailab-web
  namespace: portfolio
  labels:
    app: ailab-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ailab-web
  template:
    metadata:
      labels:
        app: ailab-web
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      nodeSelector:
        node-role.kubernetes.io/apps: "true"
      containers:
        - name: web
          image: ghcr.io/isaacwallace123/portfolio-ailab:latest
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 3000
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ailab-web
  namespace: portfolio
  labels:
    app: ailab-web
spec:
  selector:
    app: ailab-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
